import SwiftUI

enum AppStyle {
    // Colors
    static let primary = Color.blue
    static let secondaryText = Color.secondary
    
    // Text Styles
    enum TextStyle {
        static let navigationTitle = Font.title3
    }
} 